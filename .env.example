# Finit OS interactive intake — env vars
# Copy to .env.local (or set in Netlify Dashboard → Environment variables) before running.
# Never commit real keys.

# --- Anthropic (chat agent) ---
ANTHROPIC_API_KEY=

# --- Supabase (intake state store) ---
# SUPABASE_URL is the same project URL as NEXT_PUBLIC_SUPABASE_URL — duplicated
# here so the server-only intake code never has to fall back to the public
# var. The SERVICE_ROLE_KEY is server-only; never expose to the browser.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# --- ElevenLabs (speech-to-text for voice answers) ---
ELEVENLABS_API_KEY=

# --- GitHub (paying-client repo commits on intake submit) ---
# Fine-grained PAT scoped to the Finit OS GitHub org with Contents:write on
# the client repos that the intake flow needs to touch. Never use a personal
# PAT — short-lived service tokens only.
GITHUB_TOKEN=
INTAKE_GITHUB_OWNER=finit-os

# --- Email transport (DEFERRED to Phase F — not required for v1) ---
# v1 behavior: the submit handler saves the mini-report HTML to Supabase
# (mini_report_html column). The operator drafts the actual prospect email
# via gws Gmail CLI (DRAFT-only per global CLAUDE.md). No outbound auto-send.
# Phase F: re-enable Resend by setting these and re-adding sendMiniReport
# in lib/intake/mini-report.ts (placeholder comment is in that file).
# RESEND_API_KEY=
# RESEND_FROM="Finit <hello@finitsolutions.be>"

# --- Odoo CRM (contactformulier → lead) ---
# Laat leeg om enkel te mailen. De Netlify Function contact-submit maakt per
# inzending een crm.lead aan via JSON-RPC (lib/odoo/create-lead.ts).
# API-sleutel: Odoo → gebruikersmenu → Mijn profiel → Accountbeveiliging →
# Nieuwe API-sleutel. Test met: node scripts/odoo-test-lead.mjs
ODOO_URL=https://<bedrijf>.odoo.com
ODOO_DB=
ODOO_LOGIN=
ODOO_API_KEY=
# "opportunity" (standaard, komt meteen in de pipeline) of "lead"
# (vereist de instelling Leads in CRM → Configuratie → Instellingen).
ODOO_LEAD_TYPE=opportunity
# Optioneel: id van het verkoopteam en van een tag zoals "Website".
ODOO_TEAM_ID=
ODOO_TAG_ID=

# --- Operator-facing config ---
INTAKE_OPERATOR_EMAIL=alex@finitsolutions.be
# Calendly link for the lead-magnet upsell CTA in the mini-report.
CALENDLY_URL=https://calendly.com/finit-karel/30min

# --- Existing Supabase (web app already uses these) ---
# Leaving here for completeness; the intake routes prefer the server-only
# pair above (SUPABASE_URL + SERVICE_ROLE_KEY) but fall back to NEXT_PUBLIC_*
# if needed.
NEXT_PUBLIC_SUPABASE_URL=https://dcmhmqhfuhynlysocvts.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImRjbWhtcWhmdWh5bmx5c29jdnRzIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2OTcwMTg4OCwiZXhwIjoyMDg1Mjc3ODg4fQ.qEA63SdWjpEfJLVBX4lJFIA_oz-7mFyDRLJoSr-c-q4
